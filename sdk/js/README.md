# GIX JavaScript/TypeScript SDK

WASM-compatible bindings for browser and Node.js clients to interact with GIX services.

## Status

🚧 Under development

## Planned Features

- WASM build of core GIX libraries
- TypeScript type definitions
- Browser and Node.js support
- Job submission and management
- GXF envelope operations

## Usage

```typescript
// TODO: Example usage once implemented
import { GixClient } from '@gix/sdk';

const client = new GixClient();
const jobId = await client.submitJob(envelope);
```



